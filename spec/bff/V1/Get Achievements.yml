info:
  name: Get Achievements
  type: http
  seq: 1

http:
  method: GET
  url: "{{bff_base_url}}/api/v1/GetAchievements"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
